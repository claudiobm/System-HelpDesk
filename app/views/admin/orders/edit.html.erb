<h1>Editing order</h1>

<% form_for([:admin, @order]) do |f| %>
	<%= render :partial => "form", :locals => { :f => f } %>
<% end %>

<%= link_to 'Visualizar', admin_order_path(@order) %> |
<%= link_to 'Voltar', admin_orders_path %>
